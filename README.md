## This is the command for creating the app in the currrent directory

yarn create next-app@latest .

# this is how to make the folder shorter

@echo offS

# how to add update the github repo

-git add .
-git commit -m "major updates on the project "
-git push -u origin main

# reset database

yarn prisma:seed
feature/, bugfix/, release/

feature/prisma
feature/kanban_board

npx prisma migrate dev --name add_meal_planner_models

npx prisma generate
# freshsaver
